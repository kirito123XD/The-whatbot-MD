//▪CÓDIGO BY ALBERTO9883 PRROS XD▪
//▪NyanCatBot - MD▪

import util from 'util'
import fs from 'fs'
import path from 'path'
import fetch from 'node-fetch'

let handler = m => m

handler.before = async function (m, { conn, command, MessageType, text, usedPrefix }) {

if ((m.isBaileys && m.fromMe) || m.fromMe ) return true

//▪AQUÍ AGREGA LOS COMANDOS▪
let sticker1A = /Xd|xd|xD|XD/i
let sticker1B = sticker1A.exec(m.text)

let sticker2A = /./i
let sticker2B = sticker2A.exec(m.text)

let sticker3A = /./i
let sticker3B = sticker3A.exec(m.text)

let sticker4A = /./i
let sticker4B = sticker4A.exec(m.text)

let sticker5A = /./i
let sticker5B = sticker5A.exec(m.text)

let sticker6A = /./i
let sticker6B = sticker6A.exec(m.text)

let sticker7A = /./i
let sticker7B = sticker7A.exec(m.text)

let sticker8A = /./i
let sticker8B = sticker8A.exec(m.text)

let sticker9A = /./i
let sticker9B = sticker9A.exec(m.text)

let sticker10A = /./i
let sticker10B = sticker10A.exec(m.text)

let sticker11A = /./i
let sticker11B = sticker11A.exec(m.text)

let sticker12A = /./i
let sticker12B = sticker12A.exec(m.text)

let sticker13A = /./i
let sticker13B = sticker13A.exec(m.text)

let sticker14A = /./i
let sticker14B = sticker14A.exec(m.text)

let sticker15A = /./i
let sticker15B = sticker15A.exec(m.text)

let sticker16A = /./i
let sticker16B = sticker16A.exec(m.text)

let sticker17A = /./i
let sticker17B = sticker17A.exec(m.text)

let sticker18A = /./i
let sticker18B = sticker18A.exec(m.text)

let sticker19A = /./i
let sticker19B = sticker19A.exec(m.text)

let sticker20A = /./i
let sticker20B = sticker20A.exec(m.text)

let sticker21A = /./i
let sticker21B = sticker21A.exec(m.text)

let sticker22A = /./i
let sticker22B = sticker22.exec(m.text)

let sticker23A = /./i
let sticker23B = sticker23A.exec(m.text)

let sticker24A = /./i
let sticker24B = sticker24A.exec(m.text)

let sticker25A = /./i
let sticker25B = sticker25A.exec(m.text)

//Mensaje new
let texto1A = /No tengo hermana|No tengo ermana/i
let texto1B = texto1A.exec(m.text)

//▪AQUI AGREGA LOS DIRECTORIOS DEL STICKER A MANDAR▪
if (sticker1B) {
	let vn = './storage/stickers/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker2B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker3B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker4B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker5B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker6B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker7B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker8B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker9B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker10B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker11B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker12B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker13B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker14B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker15B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker16B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker17B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker18B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker19B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker20B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker21B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

if (sticker22B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker23B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker24B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else
	
if (sticker25B) {
	let vn = './src/webp/bot.webp'
	conn.sendFile(m.chat, vn, 'bot.webp', null, m, true, { type: 'webpMessage', ptt: true })
	} else

//Mensaje new
if (texto1B) {
        conn.sendMessage(m.chat, 'En ese caso el tuyo 7w7', MessageType.text, { sendEphemeral: true, quoted: m })
        }



}

module.exports = handler